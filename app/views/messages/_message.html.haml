%li.content-right__chatsholder--chatspace--onechat{data: {message: {id: message.id}}}
  .username
    = message.user.name
  .chatdate
    = message.created_at
  .chattext
    = message.text
  = image_tag(message.image)
